DATABASE_URL="postgresql://jobintel:jobintel@localhost:5432/jobintel?schema=public"
PORT=3001
CORS_ORIGIN="http://localhost:3000"
REDIS_URL="redis://localhost:6379"

# Auth — generate a real secret: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_ACCESS_SECRET="change-me"
JWT_ACCESS_EXPIRES_IN="15m"
REFRESH_TOKEN_TTL_DAYS=7

# Object storage (MinIO locally — matches docker-compose.yml)
S3_ENDPOINT="http://localhost:9000"
S3_REGION="us-east-1"
S3_ACCESS_KEY="jobintel"
S3_SECRET_KEY="jobintel123"
S3_BUCKET="jobintel"
